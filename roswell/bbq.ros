#!/bin/sh
#|-*- mode:lisp -*-|#
#| <Put a one-line description here>
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp (ql:quickload :bbq :silent t))

(defun main (&rest argv)
  (declare (ignorable argv))
  (let ((cmd (or (car argv) "")))
    (let ((items (cond ((string-equal cmd "new") (bbq:new-items (parse-integer (second argv))))
                       ((string-equal cmd "cap") (bbq:artist-cap-items (parse-integer (second argv))))
                       (t (bbq:search-items (cl-strings:join argv :separator " "))))))
      (bbq:mpc-clear-play items))))
