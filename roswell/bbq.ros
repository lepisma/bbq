#!/bin/sh
#|-*- mode:lisp -*-|#
#| Frontend for mpm-play
exec ros -Q -- $0 "$@"
|#

(ql:quickload '(:bbq) :silent t)

(defun main (&rest argv)
  (setf *random-state* (make-random-state t))
  (print "cli in developement"))
